spring:
  application:
    name: CarDiag
  datasource:
    url: jdbc:postgresql://localhost:5432/cardiag
    username: pcodes
    password: pcodes
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 100
          batch_versioned_data: true
        order_updates: true
        order_inserts: true
    database: PostgreSQL
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 813841474406-mqcd2599t1vm5q0g26g7l0afjm2a7ulf.apps.googleusercontent.com
            client-secret: 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
            scope:
              - profile
              - email
  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json

application:
  file:
    uploads:
      media-output-path: ./uploads

  embedding:
    enabled: true
  ingestion:
    enabled: true
    json-path: src/main/resources/fault.json
  security:
    jwt:
      secret: 8wWYjFpCy6Otp48mdnWDgkTjt5IjPPJqAKpqKhzAEZM=
      expiration: 86400000 #1day
      refresh-token:
        expiration: 604800000 #7 days
  openai:
    api:
      key: ${OPEN_AI_API_KEY}
      base-url: https://api.openai.com/v1
      vision-model: gpt-4o
      audio-model: gpt-4o-audio-preview
      embedding-model: text-embedding-3-small
      max-token: 1024
  voyage:
    ai:
      api-key: ${VOYAGE_API_KEY}
      url: https://api.voyageai.com/
      model: voyage-3
  youtube:
    api:
      key: ${YOUTUBE_API_KEY}
      base-url: https://www.googleapis.com
      results-per-fault: 3
      cache-ttl-days: 7
